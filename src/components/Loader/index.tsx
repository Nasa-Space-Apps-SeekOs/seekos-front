import { CircularProgress } from '@mui/material';
import './index.scss';

const Loader = () => {
    return (
        <div id="loader">
            <CircularProgress />
        </div>
    );
};

export default Loader;
